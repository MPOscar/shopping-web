import { MsTextOverflowPipe } from './ms-text-overflow.pipe';

describe('MsTextOverflowPipe', () => {
  it('create an instance', () => {
    const pipe = new MsTextOverflowPipe();
    expect(pipe).toBeTruthy();
  });
});
