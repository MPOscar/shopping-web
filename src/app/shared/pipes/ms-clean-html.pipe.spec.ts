import { MsCleanHtmlPipe } from './ms-clean-html.pipe';

describe('MsCleanHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new MsCleanHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
